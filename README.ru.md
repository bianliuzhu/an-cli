# an-cli

[简体中文](./README.zh.md) | [English](./README.md) | [Español](./README.es.md) | [العربية](./README.ar.md) | [Français](./README.fr.md) | Русский | [日本語](./README.jp.md)

## Описание

an-cli — это CLI-инструмент для фронтенда со следующими командами:

> `anl type`: CLI, который автоматически генерирует определения типов TypeScript и функции API-запросов на основе Swagger/OpenAPI JSON.

> `anl lint`: Генерирует конфигурации ESLint, Stylelint, Prettier, CommitLint и VSCode для проектов React или Vue.

> `anl git`: Настраивает локальный Git с опциями: ветки по стандарту gitflow, темы сообщений коммитов и пользовательские команды Git.

## Возможности

- `anl type`
  - 🚀 Автоматически парсит Swagger JSON
  - 📦 Генерирует файлы определений типов TypeScript
  - 🔄 Генерирует типобезопасные функции API-запросов
  - 🎯 Поддерживает path, query и body параметры
  - 📝 Автоматически генерирует enum-типов
  - 🎨 Поддерживает форматирование кода
  - ⚡️ Поддерживает загрузку файлов
  - 🛠 Гибкие опции генерации

- `anl lint`
  - 🔍 Настройка инструментов линтинга в один клик
  - 🎨 Автоматическая конфигурация ESLint
  - 🎯 Конфигурация Prettier
  - 🔄 Стандарты коммитов через CommitLint
  - 📦 Конфигурация редактора VSCode

## Установка

> Примечание
>
> Требуется глобальная установка

```bash
$ npm install anl -g

$ yarn global add anl
```

## Использование

> Совет
>
> 1. Если вы используете впервые, сначала запустите команду, посмотрите, какие изменения произойдут в проекте, затем настройте конфиг по документации и перегенерируйте до желаемого результата.
> 2. Или следуйте шагам ниже по порядку.

# Команда anl type

## Инструкция

1. Запустите команду

```bash
$ anl type
```

2. Описание конфигурационного файла

- При первом запуске `anl type` в корне проекта автоматически создается `an.config.json` (можно создать вручную).
- При запуске `anl type` ищет `an.config.json` в корне, читает его и генерирует обертку Axios, конфигурацию, список API и типы запросов/ответов.
- Параметры файла можно свободно редактировать.

3. Пример `an.config.json`

- Файл конфигурации должен находиться в корне проекта.
- Имя файла нельзя менять.
- Подробности параметров см. в разделе «Параметры конфигурации».

```json
{
	"saveTypeFolderPath": "apps/types",
	"saveApiListFolderPath": "apps/api/",
	"saveEnumFolderPath": "apps/enums",
	"importEnumPath": "../../enums",
	"swaggerJsonUrl": "https://generator3.swagger.io/openapi.json",
	"requestMethodsImportPath": "./fetch",
	"dataLevel": "serve",
	"formatting": {
		"indentation": "\t",
		"lineEnding": "\n"
	},
	"headers": {},
	"includeInterface": [
		{
			"path": "/api/user",
			"method": "get"
		}
	],
	"excludeInterface": [
		{
			"path": "/api/admin",
			"method": "post"
		}
	]
}
```

3. Обновите конфиг при необходимости и снова выполните `anl type`. Генерация пройдет по указанным настройкам.

```bash
$ anl type
```

> Примечание
>
> Если что-то непонятно, сначала выполните `anl type`, изучите сгенерированное, затем корректируйте настройки и перерабатывайте, пока не получите желаемый результат.

## Параметры конфигурации

| Параметр                 | Тип                                   | Обязат. | Описание                                        |
| ------------------------ | ------------------------------------- | ------- | ----------------------------------------------- | --- | ----------------------- |
| saveTypeFolderPath       | string                                | Да      | Путь для сохранения файлов определений типов    |
| saveApiListFolderPath    | string                                | Да      | Путь для сохранения файлов функций API-запросов |
| saveEnumFolderPath       | string                                | Да      | Путь для сохранения файлов enum-типов           |
| importEnumPath           | string                                | Да      | Путь импорта enum-типов                         |
| swaggerJsonUrl           | string                                | Да      | URL Swagger JSON                                |
| requestMethodsImportPath | string                                | Да      | Путь импорта методов запросов                   |
| dataLevel                | 'data'                                | 'serve' | 'axios'                                         | Да  | Уровень данных в ответе |
| formatting               | object                                | Нет     | Настройки форматирования кода                   |
| headers                  | object                                | Нет     | Заголовки запроса                               |
| includeInterface         | Array<{path: string, method: string}> | Нет     | Генерировать только указанные интерфейсы        |
| excludeInterface         | Array<{path: string, method: string}> | Нет     | Исключить указанные интерфейсы                  |

## Структура генерируемых файлов

- Структура формируется на основе конфигурации.

```
project/
├── apps/
│   ├── types/
│   │   ├── models/          # Все определения типов (без enum)
│   │   ├── connectors/      # Типы API (определения интерфейсов)
│   │   └── enums/           # Определения enum-типов
│   └── api/
│       ├── fetch.ts         # Реализация методов запросов
│       └── index.ts         # Функции API-запросов
```

## Примеры сгенерированного кода

### Определения типов

```typescript
declare namespace UserDetail_GET {
	interface Query {
		userId: string;
	}

	interface Response {
		id: string;
		name: string;
		age: number;
		role: UserRole;
	}
}
```

### Функция API-запроса

```typescript
import { GET } from './fetch';

/**
 * Получить детали пользователя
 */
export const userDetailGet = (params: UserDetail_GET.Query) => GET<UserDetail_GET.Response>('/user/detail', params);
```

## Дополнительные возможности

### Разбор типов

- Поддержка всех типов данных OpenAPI 3.0
- Автоматическая обработка сложных вложенных типов
- Поддержка массивов, объектов, enum и др.
- Автогенерация комментариев к интерфейсам

### Загрузка файлов

При обнаружении загрузки файлов автоматически добавляются нужные заголовки:

```typescript
export const uploadFile = (params: UploadFile.Body) =>
	POST<UploadFile.Response>('/upload', params, {
		headers: { 'Content-Type': 'multipart/form-data' },
	});
```

### Обработка ошибок

Встроена надежная обработка ошибок:

- Сообщения об ошибках парсинга
- Предупреждения при сбое генерации типов
- Обработка исключений при записи файлов

### Фильтрация интерфейсов

Управление списком генерируемых интерфейсов через конфиг:

1. Включить конкретные интерфейсы
   - Укажите их в `includeInterface`
   - Будут сгенерированы только указанные
   - Формат: массив объектов с `path` и `method`

2. Исключить конкретные интерфейсы
   - Укажите их в `excludeInterface`
   - Будут сгенерированы все остальные
   - Формат: массив объектов с `path` и `method`

Пример:

```json
{
	"includeInterface": [
		{
			"path": "/api/user",
			"method": "get"
		}
	],
	"excludeInterface": [
		{
			"path": "/api/admin",
			"method": "post"
		}
	]
}
```

Примечание: `includeInterface` и `excludeInterface` нельзя использовать одновременно. Если заданы оба, приоритет у `includeInterface`.

## Разработка

```bash
# Установка зависимостей
npm install

# Режим разработки
Нажмите F5 для отладки

# Сборка
npm run build

# Локальная ссылка для отладки
npm run blink
```

## Заметки

1. Убедитесь, что URL Swagger JSON доступен
2. Пути в конфиге указываются относительно корня проекта
3. Сгенерированные файлы перезаписывают одноименные
4. Рекомендуется добавлять сгенерированные файлы в VCS

## FAQ

1. Не удается отформатировать сгенерированные файлы типов
   - Проверьте, установлен ли Prettier
   - Убедитесь, что в корне есть конфиг Prettier

2. Неверный путь импорта в функциях запросов
   - Проверьте `requestMethodsImportPath`
   - Убедитесь, что файл метода запросов существует

# Команда anl lint

### Обзор

Быстрая настройка инструментов линтинга для фронтенда:

- ESLint — проверка кода
- Prettier — форматирование кода
- CommitLint — стандарты сообщений коммитов
- VSCode — конфигурация редактора

### Использование

```bash
$ anl lint
```

### Детали конфигурации

#### 1. ESLint

- Автоматическая установка зависимостей
- Поддержка React/Vue
- Генерация `.eslintrc.js` и `.eslintignore`
- Интеграция TypeScript

#### 2. Prettier

- Автоматическая установка зависимостей Prettier
- Генерация `.prettierrc.js`
- По умолчанию:
  - Ширина строки: 80
  - Табуляция
  - Одинарные кавычки
  - Скобки у стрелочных функций
  - Прочие правила стиля

#### 3. CommitLint

- Установка зависимостей CommitLint
- Настройка git hooks Husky
- Генерация `commitlint.config.js`
- Стандартизация сообщений коммитов

#### 4. VSCode

- Создание `.vscode/settings.json`
- Автоформатирование при сохранении
- Настройка форматтера по умолчанию
- Обновление существующих конфигов

# Команда anl git

### Обзор

Применение возможностей Git к текущему репо через интерактивный мультивыбор:

- Создание стандартных веток gitflow
  - Копировать `.gitscripts/`, `.gitconfig`, `.commit-type.js` (только если отсутствуют)
  - Сделать исполняемым `.gitscripts/random-branch.sh`
  - Выполнить `git config --local include.path ../.gitconfig`
- Автонастройка темы коммита
  - Копировать `.githooks/commit-msg` и сделать исполняемым
  - Выполнить `git config core.hooksPath .githooks`
- Пользовательские команды git
  - Добавить `.gitattributes` (только если отсутствует)

### Использование

```bash
$ anl git
```

Выберите одну или несколько функций. Файлы создаются только при отсутствии; существующие сохраняются.

### Заметки

- Запускайте внутри Git-репозитория.
- Если автоконфигурация не удалась, выполните вручную:

```bash
git config --local include.path ../.gitconfig
git config core.hooksPath .githooks
```

# Лицензия

Лицензия ISC

# Вклад

Issues и Pull Requests приветствуются: https://github.com/bianliuzhu/an-cli
